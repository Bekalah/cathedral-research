name: Secrets Placeholders (Non-Operational)
on: { workflow_dispatch: {} }
jobs:
  reference-only:
    runs-on: ubuntu-latest
    steps:
      - name: Document expected deployment secrets
        run: |
          echo "This workflow is a placeholder to reference required SWA tokens.";
          echo "Expected: SWA_TOKEN_ARCANAE_LAB=${SWA_TOKEN_ARCANAE_LAB:-unset}";
          echo "Expected: SWA_TOKEN_CATHEDRAL_OF_CIRCUITS=${SWA_TOKEN_CATHEDRAL_OF_CIRCUITS:-unset}";
          echo "Expected: SWA_TOKEN_STONE_GRIMOIRE=${SWA_TOKEN_STONE_GRIMOIRE:-unset}";
          echo "No deployments performed here."