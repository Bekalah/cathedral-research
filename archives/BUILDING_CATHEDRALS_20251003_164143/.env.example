# Server-side (DO NOT expose in client bundles)
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_KEY=your-dev-only-key
AZURE_OPENAI_DEPLOYMENT=gpt-4o-mini

# Optional future services
AZURE_SEARCH_ENDPOINT=
AZURE_SEARCH_KEY=
AZURE_SEARCH_INDEX=
AZURE_VISION_ENDPOINT=
AZURE_VISION_KEY=
AZURE_STORAGE_CONNECTION=

# Client-safe flags (no secrets)
VITE_APP_NAME=Cathedral of Circuits
VITE_APP_VERSION=1.0.0
VITE_ENABLE_DEBUG=true

# Notes:
# 1. Prefer Managed Identity in production (omit AZURE_OPENAI_KEY there).
# 2. Never commit a real key. Use Key Vault or GitHub Actions secrets.
